apiVersion: v1
kind: Service
metadata:
  name: {{ include "ax.fullname" . }}-dashboard
  labels:
    {{- include "ax.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.dashboard.port }}
      targetPort: {{ .Values.dashboard.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "ax.name" . }}
    app.kubernetes.io/component: dashboard
